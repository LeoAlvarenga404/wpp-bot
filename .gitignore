# compiled output
/dist
/node_modules
/build
/web/dist
/web/node_modules

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# temp directory
.temp
.tmp

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Baileys auth state (NEVER commit — contains session keys)
/auth_info

# Cloudflared tunnel logs
/cloudflared.log

# Affiliate cache (contains live short links, keep local)
/affiliate-links.json

# Firecrawl cache
/.firecrawl

# Local screenshots / personal data shared during dev (CPF, address, etc)
/profile.png
/image.png

# Claude Code project state
/.claude


# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Dedup log (file-backed posted-history; contains live catalog ids)
/data/
/data/posted-log.json
